// Replace this stub with real auth when backend is ready:
// import { useAuth } from '../../hooks/useAuth'
// const { user } = useAuth()
// if (!user) return <Navigate to="/admin/login" replace />

export default function AdminGuard({ children }) {
  return children
}
